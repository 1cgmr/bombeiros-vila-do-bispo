# Scripts

Diretório reservado a automações simples e independentes de fornecedor. Não são necessários scripts personalizados nesta fase; os comandos de qualidade vivem nos `package.json` de `web/` e `studio/`.
